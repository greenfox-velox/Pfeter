'use strict';

var j1 = 10;
var j2 = 3;
// tell if j1 is higher than j2 squared
// and smaller than j2 cubed
if ((j1 > j2 * j2) && (j1 < j2 * j2 * j2)) {
    console.log('j1 is higher than j2 squared and smaller than j2 cubed')
}
